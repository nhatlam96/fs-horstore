/* const dialog = document.getElementById('dialogElement');
const openBtn = document.getElementById('id_btn');

document.addEventListener('click', function(event) {
    const isClickInsideDialog = dialog.contains(event.target);
    const isClickInsideOpenBtn = openBtn.contains(event.target);
  
    if (!isClickInsideDialog && !isClickInsideOpenBtn) {
      dialog.style.display = 'none';
    }
  });
  
  openBtn.addEventListener('click', function() {
    dialog.style.display = 'block';
  });


function gree(){
    alert("hello");
}

function cart(){
    document.getElementById("").style.display = "block";
}



 */